name: Autotag and Release
on:
  push:
    branches:   
      - main
jobs:
  tag-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: pantheon-systems/action-autotag@v0.1.5
        with:
          gh-token: ${{ secrets.GITHUB_TOKEN }}
